<view>
  <framework class="framework" info="{{ info }}" menu="{{ menu }}" onChange="onChange" defaultActiveKey="{{ defaultActiveKey }}" activeKey="{{ activeKey }}">
    <router-view>
      <base slot="activity" />
      <huhu slot="huhu" />
    </router-view>
  </framework>
</view>